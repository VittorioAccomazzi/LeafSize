export const root = "/LeafSize/";
export const settingsPath  = `${root}settings`;
export const selectionPath = `${root}`;
export const processingPath= `${root}process`;
export const resultPath    = `${root}result`;
